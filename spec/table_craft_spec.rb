# frozen_string_literal: true

RSpec.describe(TableCraft) do
  it "has a version number" do
    expect(TableCraft::VERSION).not_to(be_nil)
  end

  # it "does something useful" do
  #     expect(false).to(be(true))
  #   end
end
